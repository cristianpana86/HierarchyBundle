{% extends "CPANA\HierarchyBundle::layout.html.twig"%}

{% block content %}
    {{ dump(result) }}

    {# {% for row in managers %} #}
	    <hr>
		{#  {{ row['x'].getProperty('usr_login')  }}  #}
    {#  {% endfor %}  #}

	
{% endblock %}
    
    
